fn main() {
    println!("This is an example of once_static logic.");
    println!("The once_static_system uses std::sync::Once for global one-time execution.");
    println!("It will print 'Demonstrating call_once' only once, even if the system runs multiple times.");
}